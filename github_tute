同步repository到本地

git remote -v
git remote add upstream //repository url
git remote -v
git fetch upstream
git checkout master
git merge upstream/master


本地添加到repository

git add .
git commit -m 'message string'
git push

删除后添加到repository

rm ...
git add --all
git commit -m 'delete'
git push
